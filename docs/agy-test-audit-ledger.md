# AGY-TEST-AUDIT ledger

One row per completed test-coverage audit, run after AGY-CAPSTONE was GREEN over the same range. In each
audit:
- an independent peer (agy) audited the committed test suites for gaps a regression could slip through;
- every claimed gap was verified by measurement (a logic mutant that leaves the suite green);
- the owner scoped which gaps to close;
- every closing test was proven non-vacuous by the same mutant turning that test red.

Conventions: the newest row goes at the BOTTOM, and every `|` inside a cell is escaped as `\|`.

| Date | Audited range | Rounds | Verdict | Evidence |
| --- | --- | --- | --- | --- |
| 2026-09-14 | `99422d4..8209292` (peer audited to `f343722`; `8209292` is the fold. Files: smoke.rs, support/mod.rs, src/bin/fake-agent.rs, src/main.rs) | 1 | GAPS FOUND: 6 claimed, 6 confirmed by mutant, all 6 FOLDED (owner: close all) | Fold commit `8209292` (8 to 12 tests; 7 mutants, each red on its target test); brief `.clavity/seams/sp0-test-audit.md` |
| 2026-09-14 | `5220080..81a67a5` (SP1; peers audited to `da27060`; `81a67a5` is the fold. Two subagent auditors at the owner's direction: config/CLI/output suites and launcher/adapter/console suites) | 1 | GAPS FOUND: 8 claimed, 8 confirmed by the auditors' mutants or probes, all 8 FOLDED (owner: close all) | Fold commit `81a67a5` (102 to 108 tests): lock timeout; check 5 before check 6; bare-invocation help on stderr; non-UTF-8 `AGENT_PROFILE_HOME`; dry run of an existing profile; breakaway under a controlled caller job; inherited ignore-Ctrl-C (console-driver `CONSOLE_DRIVER_JOB_LIMITS`/`CONSOLE_DRIVER_IGNORE_CTRL_C` modes). Driver re-proved all 9 mutants (`scratchpad/audit_mutants.py`: lock timeout 1 s, check swap, help to stdout, UTF-8 unwrap, `profile_dir_exists=false`, breakaway always-allow and never-allow, fixture breakaway flag dropped, wrapper clearing the ignore attribute), each failing its new test. Brief `scratchpad/test-audit-brief.md`. Discarded below the floor: the parent-environment-unchanged assertion cannot fail by construction; job-before-spawn ordering race. |
| 2026-09-15 | `6a0bab7..2234636` (SP2; auditors read `fa92d7b`; `7d4f6ba` is the fold and `2234636` its round-5 test follow-up. Two subagent auditors at the owner's direction: adapter layer, and discovery/errors/report/CLI) | 1 | GAPS FOUND: 11 claimed (one found by both auditors), 10 distinct, all 10 confirmed by the driver's re-run of each mutant (full suite green), all 10 FOLDED (owner: close all); plus one code defect: chained secret options leaked in the report (owner: fix, capstone round 5 GREEN) | Fold `7d4f6ba` (145 to 155 tests): per-adapter case-twin refusal; not-installed before the case twin; `existed` follows the declared kind (two mutants); initialization ignores a stale `existed`; Aider temp file beside its target; SECRET/PASSWORD/CREDENTIAL name parts (three mutants); padded `Authorization: Basic ...==` header; setup errors before a conflict; no verbose report on failed initialization; a configured `.com` is native; the chained-option leak. Driver proved all 14 mutants red on their named new test (`scratchpad/audit_verify.py`, PROVE group, worktree at `7d4f6ba`). Brief `.clavity/seams/sp2-test-audit.md`. Discarded below the floor: `ensure_paths` directory post-check (equivalent to `create_dir_all`); measured-evidence notes not required non-empty; `.com`/`.cmd` search order in one directory (listed limit); report origin `(PATH)` label (SP1 code). |
| 2026-09-16 | `e3f091d..14002ad` (SP3 repository resolution test suites: `repo.rs`, `config.rs`, `cli.rs`, `output.rs`, `resolve.rs`, `error.rs` unit tests; `tests/resolution.rs`, `tests/support/mod.rs`, the changed `tests/launch.rs` rows; branch `sp3-resolution`) | 1 (run on a subagent auditor at the owner's direction) | GAPS FOUND: 6, all verified by measurement and all closed (owner chose to close all) | Fold `14002ad`. Each gap was demonstrated by a change to the shipped code that left the whole suite green, and each new test was then proven red under that same change (`scratchpad/sp3_audit_mutants.py`, 6/6 RED): `unlink_removes_the_first_key_that_matches_not_the_first_stored_entry` (design §6.2 searches candidate keys in order, not stored entries), `a_key_differing_only_in_letter_case_never_matches_a_root` (§4.2 component case), `a_no_op_unlink_creates_the_application_root_and_its_lock_file_only` (what a "no write" outcome touches), `a_git_file_and_a_commondir_of_exactly_64_kib_are_accepted` (the accepting side of the §5.2 cap), a `gitdir without the space` row in `broken_git_entries_are_errors_never_the_parent_root` (near-miss prefix), and `unlink_without_repo_reports_the_discovery_error_not_not_in_a_repository` (a broken `.git` is not "not inside a Git repository"). Gate at `14002ad`: Windows nextest 228/228, Linux (WSL Ubuntu 26.04) 218/218, fmt, clippy `-D warnings`, typos clean. Brief `.clavity/seams/sp3-test-audit-r1.md`. | Discarded below the floor by the auditor: the path spelling in `run_unlink`'s "not inside a Git repository" error (message cosmetics; the `link` twin is pinned), and the canonical-versus-resolved order in `repo::unlink_keys` (an equivalent mutant: the two candidates coincide whenever the first exists). Named as unfalsifiable by the same reasoning: the "canonical then resolved" half of `unlink_keys_prefer_the_canonical_path_then_the_resolved_then_the_literal`'s name. Not audited: no `#[cfg(unix)]` test executed in the auditor's runs (Windows host), so the symlink, dangling-`.git`, FIFO, mode-000 and deleted-working-directory tests are outside its evidence; the config-changes-between-reads race could not be forced without an injection hook. Anomaly logged separately: `ProfilePresence::Known` is never constructed, so `presence:     known` is unreachable (SP2 code). |
| 2026-09-21 | `32e8673..b8999a1` (SP4a sandbox harness; the auditors read `e3574ef` and the folds carried it to `b8999a1`; folds `9dcdfb1`, `95dfa04`, `b8999a1`. Scope: the four shell suites `probe-harness.sh`, `transcript.sh`, `verify-transcripts.sh`, `resolve-agents.sh` and the sources they guard. The Rust suites were audited in the SP0-SP3 rows above and were not re-audited.) | 1 (two auditors: the agy peer, review-only, and one Opus subagent holding sole mutation rights) | GAPS FOUND: 12 verified, 11 FOLDED, 1 DEFERRED-TO-ANOMALIES. **NOTE: the capstone over this range was CLOSED BY OWNER WAIVER, not by a green round** -- nine rounds ran and none was clean, so this audit did not inherit the usual precondition. | Folds `9dcdfb1` (transcript 35 -> 39, verify-transcripts 46 -> 52), `95dfa04` (comment correction), `b8999a1` (probe-harness 139 -> 160, resolve-agents 21 -> 25); 241 -> 276 checks. Every gap was proven by a LOGIC MUTANT that left the suite GREEN, and every new test proven RED under that same mutant. **The severe one, driver-measured end to end:** dropping `transcript.sh`'s two-space section indent lets an agent's own `--version` output supply a header field, because `verify-transcripts.sh`'s `field()` scans the whole file and takes the first match -- it returned the agent's `9.9.9` instead of the harness's `1.2.3` while the suite printed "all transcript checks passed". Both auditors reached it independently. Others: a run with no behaviour step emitted a literal `baseline-*:` section label; every refusal in `run.sh` was untested, including the charset guard that stops `1.0'; id; echo PWNED; :'` reaching the harness-uid shell (driver re-ran that mutant: 9 checks red, including "the container is never told to run"); the candidate sweep's own restore; ten of eleven credential-filter categories; `probe_excerpt`'s 500-character line bound; `PROBE_TARGET`'s default, the unpinned third of a trio; the orphan loop's README exemption; the agent-cap boundary; baseline identity vs count; and the two `gh api` refusal branches. Brief `.clavity/seams/sp4a-test-audit.md`; run report `.clavity/scratch/sp4a-test-audit/run-report.md`. DEFERRED: a repeated `probe_candidates` sweep silently destroys the first -- untestable without a source change that would invalidate the capstone, owner declined, logged in `.clavity/local-anomalies.md` 2026-09-21. Discarded below the floor by the auditors: `probe_restore_default`'s catastrophic-argument guard (every caller already filters); a mechanism-label collision needing a verbatim duplicate call; `verify-transcripts.sh`'s 40-hex length check (redundant behind the SHA equality). Coverage note carried forward: `resolve-agents.sh`'s charset check passes on this host only because its filesystem is CASE-INSENSITIVE -- on the Linux runner the `probe-tests` job targets, which has never run, that check would be vacuous. |
