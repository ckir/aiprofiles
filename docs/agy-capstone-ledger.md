# AGY-CAPSTONE ledger

One row per completed capstone review of a committed implementation. In each review:
- an independent peer (agy) tore down the code a plan produced;
- every finding was verified by measurement before it was folded;
- the owner adjudicated GREEN.

The evidence column names things a reader can check: fold commits and the review transcript.

| Date | Range reviewed | Rounds | Verdict | Evidence | Discarded or deferred (anti-sweep) |
|---|---|---|---|---|---|
| 2026-09-13 | `99422d4..0e668d3`, excluding `Cargo.lock` and `docs/superpowers/` (SP0 scaffold; branch `sp0-scaffold`) | 2 | GREEN, owner-confirmed at `0e668d3` | No capstone folds were needed; the pre-capstone code-review fold is `dfc50b7` (fixture tests isolated from the inherited `FAKE_AGENT_EXIT`/`FAKE_AGENT_ECHO_ENV`). Transcript: agy cascade `07220325-ce4e-44cc-8887-889ff6dff33c`. Gate at `0e668d3`: `just check` passed on a clean build (8/8 tests); `cargo deny` ok; `cargo +1.85 check` ok; docs build with `-D warnings` ok; actionlint 0 errors. | Four blocking claims were rejected by measurement. (1) `actions/checkout@v7` does not exist: the `refs/tags/v7` tag exists and flux CI is green with it. (2) Dependabot auto-merge needs `pull_request_target`: GitHub's official recipe is `on: pull_request` with write permissions. (3) Reserving exit 125 blocks a 125 passthrough test: `FAKE_AGENT_EXIT=125` exits 125 with JSON on stdout, while a fixture error has empty stdout. (4) The stub's catch-all argument forces an SP1 rewrite: this is by design, since design §3.2 (lines 98–99) assigns the grammar to SP1. Linux and macOS first-run behaviour was reasoned, not measured; the PR run in plan Task 9 Step 1 measures it. |
| 2026-09-13 | `f77ed4a..1881aa1`, excluding `Cargo.lock` and `docs/superpowers/` (SP0 fold after PR #1's macOS CI failure; branch `sp0-scaffold`) | 1 (round 3 of this SP; reply restated after a stalled command-approval prompt) | GREEN, owner-confirmed at `1881aa1` | Fold under review: `6e35bc8` (`fake-agent` is now `crates/agent-profile/src/bin/fake-agent.rs`; tests use `CARGO_BIN_EXE_fake-agent`, with no nested cargo), `8a7686d` (MSRV 1.98), merge `f044faa` (`.antigravityignore`). Transcript: agy cascade `07220325-ce4e-44cc-8887-889ff6dff33c`. Gate on Windows at `8a7686d`: `just check` 8/8; `cargo +1.98 check` ok. Peer measured `cargo +stable --version` = 1.98.0. | No findings raised. The peer marked unverified whether the shipped binary links `serde_json`; that is not a defect. It examined `.github/dependabot.yml` least; that file is unchanged in this range. Not measured: macOS (no machine) and Linux after the fold (the WSL VM stopped starting). The PR #1 CI re-run measures both. |
| 2026-09-14 | `5220080..0fb497c`, excluding `Cargo.lock` and `docs/superpowers/` (SP1 core and explicit launch; branch `sp1-core-launch`) | 4 (run on subagent reviewers at the owner's direction; agy peer unavailable) | GREEN, owner-confirmed at `0fb497c` | Folds: `4b1a12d` (once-per-process Windows launch; relative home directory is an `AppRoot` error; concurrent-writer values checked; `FAKE_AGENT_BREAKAWAY` fixture and direct-vs-wrapper breakaway test), `10cde83` (job copies the caller job's breakaway setting instead of hard-coding it), `225a66b` (silent breakaway in the caller job counts as allowing breakaway), docs `b889994`, `2dfa10d`, `0fb497c`. Every fold proven by a logic mutant (breakaway: always-allow fails under `cargo test`, never-allow fails under nextest, silent row fails its unit test); PowerShell job probe (`scratchpad/silent.ps1`): direct and wrapped exit codes equal for caller jobs with no flag, `BREAKAWAY_OK`, `SILENT_BREAKAWAY_OK`, `KILL_ON_JOB_CLOSE\|SILENT_BREAKAWAY_OK`. Gate on Windows at `0fb497c`: nextest 102/102, fmt, clippy `-D warnings`, typos clean. | Deferred debt: `console-driver` pipe-drain threads are not bounded by its exit timeout when a descendant holds the pipes (no current test combines a console event with a sleeper; logged in TODO.md). Discarded below the floor: close-event delivery order unmeasured (documented untested path); `DuplicateHandle` failure silently losing the close guarantee (resource exhaustion only); `LAUNCHED` poisoning after a failed first launch (single-launch binary). Accepted limit: with a silent-breakaway caller job the agent's ordinary descendants stay under the caller job's limits (design §11). Not measured after the folds: Linux and macOS (WSL VM failed to start); the SP1 PR CI run measures both. |
| 2026-09-15 | `6a0bab7..fa92d7b`, excluding `Cargo.lock` and `docs/superpowers/` (SP2 adapter architecture gate; branch `sp2-adapters`) | 4 (run on subagent reviewers at the owner's direction) | GREEN, owner-confirmed at `fa92d7b` | Folds: `86e23e5` (a configured `.cmd`/`.bat`/`.ps1` path with trailing spaces or dots is refused; Windows strips them and std ran it through cmd.exe), `f492536` (README: a configured `.com` is native), `aff9dc1` (owner-approved after an AGY-FIRST consult on subagents: report redacts secret argument values by name rule; launched argv unchanged), `c35cf7f` (dotted Codex keys, `Name: value` headers, no `--no-*` exemption; unknown-option usage error echoes only the name), `4f83ed5` (dotted segments are TOML bare keys with `-`; header value with `:` and option value with `==` pinned), docs `c725984`, `85f2fe8`, `fa92d7b`. Every fold proven by a logic mutant (trim removed; `HidesNext` to `Verbatim`; name rule off; assignment rule off; `.` split off; header rule off; `split_once` to `rsplit_once` in cli and header; hyphen segments rejected). Gate on Windows at `fa92d7b`: nextest 145/145, fmt, clippy `-D warnings`, typos clean; the identical-code prototype passed CI run 34926167302 on Windows, Linux and macOS before the folds. | Owner-accepted debt: the Aider writer's temp-file `sync_all` before the rename is untested (a mutant removing it leaves the suite green; durability needs crash injection). Known limits recorded in design §10 and TODO.md: positional, inline-JSON and non-sensitive-named secrets; abbreviations dropping the sensitive part; an invalid UTF-8 byte splitting a sensitive name part (below the floor: no documented agent form); over-redaction; the `ArgumentConflict` echo. Discarded below the floor: Windows alternate-data-stream executable names; spaces around `=`; clustered short `-c` values. Pre-existing, logged as an anomaly: `windows_console` `breakaway_follows_a_controlled_caller_job` fails under plain `cargo test` (also at `3281f61`) and passes under nextest. Not measured after the folds: Linux and macOS; the SP2 PR CI run measures both. |
